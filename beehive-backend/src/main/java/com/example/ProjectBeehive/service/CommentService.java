/*
package com.example.ProjectBeehive.service;

import com.example.ProjectBeehive.payload.CommentDto;

import java.math.BigInteger;
import java.util.List;

public interface CommentService {
    CommentDto createComment(BigInteger postId, CommentDto commentDto);

    List<CommentDto> getCommentsByPostId(BigInteger postId);

    CommentDto getCommentById(BigInteger postId, BigInteger commentId);

    CommentDto updateComment(BigInteger postId, BigInteger commentId, CommentDto commentRequest);

    void deleteComment(BigInteger postId, BigInteger commentId);
}
*/
